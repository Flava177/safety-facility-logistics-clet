package gh.edu.clet.sfl.fleetlogistics.fleet.domain.model;

/** Workflow severity — how bad the consequence is, as distinct from how soon it must be handled. */
public enum WorkflowSeverity {
    MINOR,
    MODERATE,
    MAJOR,
    CRITICAL
}

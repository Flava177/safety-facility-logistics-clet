package gh.edu.clet.sfl.fleetlogistics.fleet.domain.model;

/** Approval state for an evidence export request (SRS-SFL-S166-03). */
public enum EvidenceExportStatus {
    REQUESTED,
    APPROVED,
    REJECTED,
    EXPORTED
}

package gh.edu.clet.sfl.fleetlogistics.fleet.domain.model;

/** Workflow priority; an SLA input alongside severity, site, operating mode and workflow type. */
public enum WorkflowPriority {
    LOW,
    MEDIUM,
    HIGH,
    URGENT
}

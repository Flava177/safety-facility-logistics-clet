package gh.edu.clet.sfl.safetysecurity.cctv.domain.model;

/** Mirrors {@code accesscontrol.domain.model.ExceptionStatus} - each module keeps its own copy. */
public enum AlertStatus {
    OPEN,
    ACKNOWLEDGED,
    RESOLVED
}

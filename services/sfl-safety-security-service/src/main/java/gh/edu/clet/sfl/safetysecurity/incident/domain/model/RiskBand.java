package gh.edu.clet.sfl.safetysecurity.incident.domain.model;

/** The banded output of a {@link RiskRating}'s likelihood x impact score. */
public enum RiskBand {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}

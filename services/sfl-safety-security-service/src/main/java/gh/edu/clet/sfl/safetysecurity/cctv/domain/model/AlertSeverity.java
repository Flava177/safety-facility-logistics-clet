package gh.edu.clet.sfl.safetysecurity.cctv.domain.model;

/** Mirrors {@code accesscontrol.domain.model.ExceptionSeverity} - each module keeps its own copy. */
public enum AlertSeverity {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}

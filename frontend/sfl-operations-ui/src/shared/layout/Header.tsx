/**
 * The application bar moved to `TopBar`, which spans the full width above the navigation rail.
 * This module is kept only so an older import path keeps resolving; it adds no behaviour.
 */
export { default } from './TopBar';

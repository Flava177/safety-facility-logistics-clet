services:
  facilities-postgres:
    image: postgres:16-bookworm
    container_name: sfl-facilities-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sfl_facilities_service
      POSTGRES_USER: sfl
      POSTGRES_PASSWORD: sfl
    ports:
      - "5441:5432"
    volumes:
      - sfl_facilities_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sfl -d sfl_facilities_service"]
      interval: 10s
      timeout: 5s
      retries: 10

  safety-security-postgres:
    image: postgres:16-bookworm
    container_name: sfl-safety-security-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sfl_safety_security_service
      POSTGRES_USER: sfl
      POSTGRES_PASSWORD: sfl
    ports:
      - "5442:5432"
    volumes:
      - sfl_safety_security_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sfl -d sfl_safety_security_service"]
      interval: 10s
      timeout: 5s
      retries: 10

  fleet-vehicle-postgres:
    image: postgres:16-bookworm
    container_name: sfl-fleet-vehicle-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sfl__fleet_vehicle_service
      POSTGRES_USER: sfl
      POSTGRES_PASSWORD: sfl
    ports:
      - "5443:5432"
    volumes:
      - sfl_fleet_vehicle_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sfl -d sfl__fleet_vehicle_service"]
      interval: 10s
      timeout: 5s
      retries: 10



  facilities-e2e-postgres:
    image: postgres:16-bookworm
    container_name: sfl-facilities-e2e-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sfl_facilities_service_e2e
      POSTGRES_USER: sfl
      POSTGRES_PASSWORD: sfl
    ports:
      - "55441:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sfl -d sfl_facilities_service_e2e"]
      interval: 10s
      timeout: 5s
      retries: 10

  safety-security-e2e-postgres:
    image: postgres:16-bookworm
    container_name: sfl-safety-security-e2e-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sfl_safety_security_service_e2e
      POSTGRES_USER: sfl
      POSTGRES_PASSWORD: sfl
    ports:
      - "55442:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sfl -d sfl_safety_security_service_e2e"]
      interval: 10s
      timeout: 5s
      retries: 10

  fleet-vehicle-e2e-postgres:
    image: postgres:16-bookworm
    container_name: sfl-fleet-vehicle-e2e-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sfl__fleet_vehicle_service_e2e
      POSTGRES_USER: sfl
      POSTGRES_PASSWORD: sfl
    ports:
      - "55443:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sfl -d sfl__fleet_vehicle_service_e2e"]
      interval: 10s
      timeout: 5s
      retries: 10



volumes:
  sfl_facilities_postgres_data:
  sfl_safety_security_postgres_data:
  sfl_fleet_vehicle_postgres_data:
